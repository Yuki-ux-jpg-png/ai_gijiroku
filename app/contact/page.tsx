'use client';
import {useState} from 'react';
export default function Contact(){const[msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setMsg('送信しました。');}return <main className="section"><div className="container"><div className="card"><h1>お問い合わせページ</h1><form className="form" onSubmit={submit}><input name="name" placeholder="お名前" required/><input name="email" type="email" placeholder="メールアドレス" required/><select name="type"><option>サービスについて</option><option>請求・解約について</option><option>データ削除について</option><option>その他</option></select><textarea name="message" placeholder="お問い合わせ内容" rows={7} required/><button className="btn">送信</button></form>{msg&&<p>{msg}</p>}</div></div></main>}
